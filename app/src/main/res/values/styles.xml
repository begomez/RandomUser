<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Details">
        <item name="android:windowSharedElementEnterTransition">@transition/shared_photo</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ItemTitle" parent="TextAppearance.AppCompat.Title">
        <item name="android:maxLines">1</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="ItemSubtitle" parent="TextAppearance.AppCompat.Caption">
        <item name="android:maxLines">1</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="ItemBody" parent="TextAppearance.AppCompat.Body1">
        <item name="android:gravity">left</item>
    </style>

    <style name="DetailBody" parent="ItemBody">
        <item name="android:paddingLeft">@dimen/item_pad_horiz</item>
        <item name="android:paddingStart">@dimen/item_pad_horiz</item>
        <item name="android:paddingTop">@dimen/item_pad_vert</item>
    </style>

</resources>
